version: 2
jobs:
  build:
    docker:
      - image: debian:buster
    steps:
      - checkout
      - run:
          name: Install debian-packaged dependencies
          command: |
            apt update
            apt install -y git ldc dub build-essential
            ln -s /usr/bin/ldc2 /usr/bin/ldc
      - run:
          name: Install btest
          command: |
            git clone https://github.com/eatonphil/btest
            cd btest
            make
            make install
      - run:
          name: Install bsdscheme
          command: |
            make
            make install
      - run:
          name: Run bsdscheme tests
          command: btest
